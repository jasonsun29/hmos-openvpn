# HarmonyOS 调试签名配置模板
# 复制此文件为 local.properties（已在 .gitignore 中排除），按本机实际路径填写：
# 1. signing.dir: DevEco-Studio 自动签名目录，macOS 默认 ${HOME}/.ohos/config
# 2. signing.cert.name: 自动生成的证书 hash（不含后缀），可执行 `ls ~/.ohos/config/default_*.cer` 查看
#
# 注意：本文件无需随仓库分发，build-profile.json5 通过 ${signing.dir}/${signing.cert.name}.cer
# 占位符引用 local.properties 的键值。
signing.dir=${HOME}/.ohos/config
signing.cert.name=default_app_XXXX_REPLACE_WITH_YOUR_CERT_HASH